-- STUB: typed, empty output matching data/normalized/tournament_team_member.schema.json.
-- Phase 2 replaces this with real stg_munchstats roster normalization logic,
-- mapping roster Pokémon names to pokemon_key/pokemon_id.
select
  cast(null as varchar) as team_member_id,
  cast(null as varchar) as team_id,
  cast(null as varchar) as event_id,
  cast(null as varchar) as pokemon_key,
  cast(null as integer) as pokemon_id,
  cast(null as integer) as slot_number,
  cast(null as varchar) as source_name,
  cast(null as varchar) as source_url,
  cast(null as varchar) as source_record_id,
  cast(null as varchar) as extracted_at_utc,
  cast(null as varchar) as dataset_version
where false

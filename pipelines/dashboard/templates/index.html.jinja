<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Pokémon Champions — Analytics Dashboard</title>
<style>
  /* ---------------------------------------------------------------
   * Design tokens — see docs/design-system.md for the full token
   * reference, component catalog, and Pokémon-representation/naming
   * conventions this dashboard follows. Every color, spacing, and
   * radius value used below traces back to one of these variables so
   * the two stay in sync.
   * --------------------------------------------------------------- */
  :root {
    /* Color */
    --navy: #16264d;
    --blue: #2b5cad;
    --light-blue: #eaf1fb;
    --border: #16264d;
    --muted: #5b6b8c;
    --bg: #f4f6fb;
    --surface: #fff;
    --positive: #1f8a4c;
    --positive-bg: #e8f7ee;
    --warning: #a8710a;
    --warning-bg: #fbf1e0;
    --danger: #b23434;
    --danger-bg: #fbe9e9;

    /* Broadcast/esports theme tokens (docs/design-system.md's "Broadcast
       color-block header" component) — color-blocking + weight/case is
       what gives this a broadcast-graphic feel, deliberately without
       adding a webfont dependency. */
    --panel-dark: #12131a;
    --accent-red: #e3323c;
    --accent-red-bg: #fce7e8;
    --accent-gold: #f4b942;
    --accent-gold-bg: #fdf3dd;

    /* Icon-size scale (docs/design-system.md's "Icon size scale") — every
       sprite/item/type icon must use one of these three tokens, no other
       pixel value, replacing four previously ad-hoc sizes (40/32/24/22px).
       --icon-xl is a deliberate 4th tier, reserved for the Pokémon Profile
       header's dual-type badge (a new, hero-level piece of information —
       see "Larger type badge" in docs/design-system.md). */
    --icon-sm: 40px;
    --icon-md: 64px;
    --icon-lg: 96px;
    --icon-xl: 128px;

    /* Per-type accent colors (docs/design-system.md's "Type color
       accents"). Emitted from pipelines/render/template.py's TYPE_COLORS
       via build.py's _type_colors() rather than hardcoded here, so the
       dashboard and the team-card renderer share one palette. Used as
       accents (borders, gradients) rather than fills — the broadcast
       palette above stays the page's identity. */
    {%- for type_name, hex in payload.type_colors.items() | sort %}
    --type-{{ type_name }}: {{ hex }};
    {%- endfor %}
    --type-unknown: #6B7280;

    /* Speed-tier badge scale (docs/design-system.md's "Speed-tier badge"
       component) — bucketed off pokemon_champions_profile.speed. */
    --speed-blazing: #b23434;
    --speed-blazing-bg: #fbe9e9;
    --speed-fast: #a8710a;
    --speed-fast-bg: #fbf1e0;
    --speed-average: #2b5cad;
    --speed-average-bg: #eaf1fb;
    --speed-slow: #5b6b8c;
    --speed-slow-bg: #eef0f5;

    /* Spacing scale (4px base unit) */
    --space-1: 4px;
    --space-2: 8px;
    --space-3: 12px;
    --space-4: 16px;
    --space-5: 20px;
    --space-6: 24px;
    --space-8: 32px;

    /* Radius scale */
    --radius-sm: 6px;
    --radius-md: 10px;
    --radius-pill: 999px;

    /* Type scale */
    --font-xs: 0.8rem;
    --font-sm: 0.85rem;
    --font-base: 0.9rem;
    --font-md: 0.95rem;
    --font-lg: 1.1rem;
    --font-xl: 1.5rem;
    --font-2xl: 1.6rem;
  }
  * { box-sizing: border-box; }
  body {
    margin: 0;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    background: var(--bg);
    color: var(--navy);
  }
  header {
    background: var(--panel-dark);
    color: #fff;
    padding: 24px;
    text-align: center;
  }
  header h1 {
    margin: 0;
    font-size: 1.6rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    font-weight: 800;
  }
  header p { margin: 4px 0 0; color: #b8bdd1; font-size: 0.9rem; }
  main { max-width: 1100px; margin: 0 auto; padding: 24px; }

  .kpi-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 16px;
    margin-bottom: 32px;
  }
  .kpi-card {
    background: #fff;
    border: 2px solid var(--border);
    border-radius: 10px;
    padding: var(--space-5);
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }
  .kpi-card .kpi-sprite { width: var(--icon-lg); height: var(--icon-lg); object-fit: contain; flex: 0 0 auto; }
  .kpi-card .kpi-body { min-width: 0; }
  .kpi-card .label { font-size: 0.8rem; color: var(--muted); text-transform: uppercase; letter-spacing: 0.03em; }
  .kpi-card .value { font-size: 1.5rem; font-weight: 700; margin-top: 4px; overflow-wrap: break-word; }
  .kpi-card .sub { font-size: 0.85rem; color: var(--muted); margin-top: 2px; }

  /* --- Tabs --- */
  .tabs {
    display: flex;
    gap: 4px;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    border-bottom: 2px solid var(--border);
    margin-bottom: 20px;
  }
  .tab-btn {
    flex: 0 0 auto;
    background: none;
    border: none;
    border-bottom: 3px solid transparent;
    color: var(--muted);
    font-size: 0.95rem;
    font-weight: 600;
    padding: 10px 16px;
    cursor: pointer;
    white-space: nowrap;
  }
  .tab-btn:hover { color: var(--navy); }
  .tab-btn[aria-selected="true"] {
    color: var(--accent-red);
    border-bottom-color: var(--accent-red);
  }
  .tab-panel[hidden] { display: none; }

  /* --- Sub-tabs: a compact pill row nested inside one top-level tab/
     section (e.g. Usage's leaderboards, Pokémon Profile's Items/Ability/
     Moves/Team Cores), so several equally-important views can share one
     section instead of stacking. Deliberately visually lighter than the
     top-level `.tabs` row above, which stays reserved for the page's
     ten main tabs. --- */
  .subtabs {
    display: flex;
    gap: var(--space-2);
    flex-wrap: wrap;
    margin-bottom: var(--space-4);
  }
  .subtab-btn {
    flex: 0 0 auto;
    background: var(--light-blue);
    border: none;
    border-radius: var(--radius-pill);
    color: var(--muted);
    font-size: var(--font-sm);
    font-weight: 600;
    padding: 6px 14px;
    cursor: pointer;
    white-space: nowrap;
  }
  .subtab-btn:hover { color: var(--navy); }
  .subtab-btn[aria-selected="true"] { background: var(--blue); color: #fff; }
  .subtab-panel[hidden] { display: none; }

  section {
    background: #fff;
    border: 2px solid var(--border);
    border-radius: 10px;
    padding: 0 var(--space-8) var(--space-8);
    margin-bottom: var(--space-6);
    overflow: hidden;
  }
  /* Broadcast color-block section header (docs/design-system.md) — bold,
     uppercase, letter-spaced, on a dark bar, replacing plain
     text-on-white section titles. */
  section h2 {
    margin: 0 calc(var(--space-8) * -1) var(--space-4);
    padding: var(--space-4) var(--space-8);
    background: var(--panel-dark);
    color: #fff;
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    font-weight: 800;
  }
  section h3 { font-size: var(--font-md); text-transform: uppercase; letter-spacing: 0.03em; color: var(--muted); }
  .controls { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 16px; align-items: flex-end; }
  .controls label { font-size: 0.85rem; color: var(--muted); display: flex; flex-direction: column; gap: 4px; }
  select {
    padding: 6px 8px;
    border-radius: 6px;
    border: 1px solid var(--border);
    font-size: 16px; /* prevents iOS Safari auto-zoom-on-focus */
  }
  /* Range filter (min/max pair — stat-range, usage/win-rate range) */
  .range-filter { display: flex; align-items: center; gap: 4px; }
  .range-filter input[type="number"] {
    width: 4.5em;
    padding: 6px 8px;
    border-radius: 6px;
    border: 1px solid var(--border);
    font-size: 16px;
  }
  .type-filter-row { display: flex; flex-wrap: wrap; gap: 4px; max-width: 320px; }

  .table-scroll { overflow-x: auto; -webkit-overflow-scrolling: touch; }
  table { width: 100%; border-collapse: collapse; font-size: 0.9rem; }
  th, td { text-align: left; padding: 6px 10px; border-bottom: 1px solid var(--light-blue); white-space: nowrap; }
  th { color: var(--muted); font-weight: 600; }
  th.sortable { cursor: pointer; user-select: none; }
  th.sortable:hover { color: var(--navy); }
  th.sortable .sort-indicator { margin-left: 4px; color: var(--accent-red); }

  /* Sprite/icon cells populated by app.js's pokemonCell()/iconCell() helpers */
  .cell-with-icon { display: flex; align-items: center; gap: 8px; }
  .cell-icon { width: var(--icon-sm); height: var(--icon-sm); object-fit: contain; flex: 0 0 auto; }

  /* Small muted annotation next to a value (Win rate leaders table's
     "(n=X)" sample-size note — see "Percentages, not raw counts" in
     docs/design-system.md). */
  .ranked-value { color: var(--muted); font-weight: 700; white-space: nowrap; }

  /* --- 6-wide grid (docs/design-system.md's "3-tier tab layout
     convention" tier 2, and the ".grid-6xn component"): the dashboard's
     one component for any usage/win-rate metric and for a spotlight
     grid of Pokémon (Overview's Top 12), replacing the old ranked-list
     bars and the separate spotlight-grid/spotlight-card pattern. Fixed 6
     columns on desktop (not auto-fill/auto-fit — a deliberately denser,
     more uniform grid), collapsing to 3 columns at 720px and 2 at 480px
     per the existing breakpoint convention. Each tile bolds its headline
     percentage (.grid-6xn-value) per the "bold the percentage" ask. */
  .grid-6xn { display: grid; grid-template-columns: repeat(6, 1fr); gap: var(--space-3); }
  .grid-6xn-tile {
    border: 2px solid var(--border);
    border-radius: var(--radius-md);
    padding: var(--space-4);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-1);
    position: relative;
  }
  .grid-6xn-tile.is-leader { border-color: var(--accent-red); background: var(--accent-red-bg); }

  /* Type color accent (docs/design-system.md's "Type color accents"): a
     left-edge bar in the Pokémon's own type colors, painted from the
     --tile-accent custom property app.js sets per element. An accent, not
     a fill — the broadcast palette stays the page's identity, and the
     .is-leader treatment above still reads over the top of it. */
  .has-type-accent { position: relative; overflow: hidden; }
  .has-type-accent::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 6px;
    background: var(--tile-accent, var(--type-unknown));
  }

  /* A team drawn as its six Pokémon (app.js's teamCompositionHtml). Wraps
     to two rows at narrow widths rather than overflowing its tile. */
  .team-comp {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-1);
  }
  .team-comp-slot { display: inline-flex; align-items: center; }
  .team-comp-slot img { display: block; }
  /* Placeholder for a roster slot whose sprite didn't resolve, so a
     six-Pokémon team still reads as six. */
  .team-comp-blank {
    display: inline-block;
    width: var(--icon-sm);
    height: var(--icon-sm);
    border-radius: 50%;
    background: var(--light-blue);
  }

  /* Hero art (pokemon_asset's home_render kind) is a transparent-background
     render, drawn at --icon-lg/--icon-xl. Unlike the menu sprites it is
     downscaled rather than upscaled, so it needs no rendering hint. */
  .hero-art { object-fit: contain; }

  /* Country flag glyph. Sized up slightly against the code beside it,
     which always stays visible — see countryCell() in app.js. */
  .country-flag { font-size: 1.15em; line-height: 1; }
  /* Normal flow, left-aligned within the centered column — not absolutely
     positioned, so it reserves its own row instead of overlapping the
     label when a tile has no icon (e.g. an item/ability/move/team with no
     resolvable sprite). */
  .grid-6xn-tile .badge-rank { align-self: flex-start; }
  .grid-6xn-tile img { width: var(--icon-md); height: var(--icon-md); object-fit: contain; }
  .grid-6xn-tile .grid-6xn-label { font-weight: 600; font-size: var(--font-xs); overflow-wrap: anywhere; }
  .grid-6xn-value { font-weight: 800; font-size: var(--font-lg); color: var(--navy); }
  .grid-6xn-tile.is-leader .grid-6xn-value { color: var(--accent-red); }
  .grid-6xn-sub { font-size: var(--font-xs); color: var(--muted); }

  /* --- Pro Team Gallery card grid --- */
  .gallery-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: var(--space-4); }
  .gallery-card { border: 2px solid var(--border); border-radius: var(--radius-md); overflow: hidden; display: flex; flex-direction: column; }
  .gallery-card img.gallery-card-image { width: 100%; display: block; }
  .gallery-card .gallery-card-body { padding: var(--space-4); display: flex; flex-direction: column; gap: var(--space-1); }
  .gallery-card .gallery-card-player { font-weight: 700; }
  .gallery-card .gallery-card-meta { font-size: var(--font-xs); color: var(--muted); }

  .empty-state {
    background: var(--light-blue);
    border-radius: 8px;
    padding: 14px;
    color: var(--muted);
    font-size: 0.9rem;
  }

  /* --- Badge (rank pills, speed tiers — see docs/design-system.md) --- */
  .badge {
    display: inline-block;
    padding: 2px 8px;
    border-radius: var(--radius-pill);
    font-size: var(--font-xs);
    font-weight: 600;
    white-space: nowrap;
  }
  .badge-rank { background: var(--light-blue); color: var(--blue); }
  .badge-speed-blazing { background: var(--speed-blazing-bg); color: var(--speed-blazing); }
  .badge-speed-fast { background: var(--speed-fast-bg); color: var(--speed-fast); }
  .badge-speed-average { background: var(--speed-average-bg); color: var(--speed-average); }
  .badge-speed-slow { background: var(--speed-slow-bg); color: var(--speed-slow); }
  .badge-positive { background: var(--positive-bg); color: var(--positive); }
  .badge-negative { background: var(--danger-bg); color: var(--danger); }

  /* Data & Sources tab. Table cells there hold prose and endpoint URLs
     rather than the short numeric values every other table has, so they
     opt into wrapping instead of forcing the row wider. */
  .wrap-cell { white-space: normal; min-width: 220px; max-width: 420px; }
  .wrap-cell code { font-size: var(--font-xs); word-break: break-all; }
  .caveat-list {
    margin: 0;
    padding-left: var(--space-5);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    color: var(--muted);
  }
  .caveat-list strong { color: var(--navy); }
  /* Freshness chip in the page header: dataset version + snapshot date,
     visible without opening the Data & Sources tab. */
  .header-meta {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-2);
    margin-top: var(--space-2);
  }
  .header-chip {
    border: 1px solid rgba(255, 255, 255, 0.35);
    border-radius: var(--radius-pill);
    padding: 2px var(--space-3);
    font-size: var(--font-xs);
    font-weight: 700;
    letter-spacing: 0.02em;
  }
  .badge-new { background: var(--accent-gold-bg); color: var(--warning); }

  /* --- Type badge (Pokémon Profile header, Matchup tab pickers) — the
     dashboard's first-ever Pokémon-type display (pokemon.type_1/type_2,
     PokéAPI). Icon-only emblems (no visible text label) — the type name
     is exposed via the pill's aria-label/the icon's title attribute
     instead, so it stays accessible without cluttering the badge. Uses
     --icon-xl for hero-level prominence, the one documented exception to
     the 3-tier icon scale. --- */
  .type-badge-row { display: flex; align-items: center; gap: var(--space-2); }
  .type-pill { display: inline-flex; align-items: center; justify-content: center; }

  /* Profile header's dual-type display — the one place a type icon uses
     --icon-xl instead of the sm/md/lg scale (see "Icon size scale" above). */
  .type-badge-row.type-badge-lg { gap: var(--space-3); margin: var(--space-2) 0; }
  /* A --icon-md emblem in a ring of its own type color: enough presence to
     read as hero-level information without competing with the Pokémon's
     hero art beside it (see renderTypeBadgeRow in app.js). */
  .type-badge-lg .type-pill-lg {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    padding: var(--space-1);
    border: 3px solid var(--pill-type-color, var(--type-unknown));
    background: var(--surface);
  }

  /* Crops static/icons/types/*.png (wide 200x40 "icon + type name" badges,
     not bare square icons) down to just the icon's fixed-width left
     square, so every type-icon use (compact pills, the large Profile
     badge, the Matchup effectiveness grid, filter chips) shows a real
     icon-only emblem instead of a squished text badge — see
     typeIconImg() in app.js. */
  .type-icon-crop { display: inline-block; overflow: hidden; border-radius: var(--radius-sm); vertical-align: -3px; }
  .type-icon-crop img { display: block; }

  /* --- Descriptive sub-text under a name (Pokémon Profile's Items/
     Ability/Moves sections; Matchup panel move picker) --- */
  .desc-text { font-size: var(--font-xs); color: var(--muted); margin-top: 2px; }

  /* --- Matchup tab: type-effectiveness grid + damage calculator --- */
  .type-effect-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(90px, 1fr)); gap: var(--space-2); }
  .type-effect-tile {
    border-radius: var(--radius-sm);
    padding: var(--space-2);
    text-align: center;
    font-size: var(--font-xs);
    font-weight: 700;
    text-transform: uppercase;
  }
  .type-effect-4x { background: #1f8a4c; color: #fff; }
  .type-effect-2x { background: #bfe3cd; color: #1f8a4c; }
  .type-effect-1x { background: var(--light-blue); color: var(--muted); }
  .type-effect-half { background: #fbe3c9; color: var(--warning); }
  .type-effect-quarter { background: var(--danger-bg); color: var(--danger); }
  .type-effect-0x { background: var(--navy); color: #fff; }

  .matchup-picker-grid { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-4); }
  .matchup-side { border: 1px solid var(--light-blue); border-radius: var(--radius-md); padding: var(--space-4); }
  .matchup-side h4 { margin: 0 0 var(--space-2); }
  .stage-row { display: flex; align-items: center; justify-content: space-between; gap: var(--space-2); margin-bottom: var(--space-1); }
  .stage-row label { font-size: var(--font-xs); color: var(--muted); flex: 0 0 60px; }
  .stage-row input[type="range"] { flex: 1 1 auto; }
  .stage-row .stage-value { width: 2.2em; text-align: right; font-weight: 700; font-size: var(--font-xs); }

  .damage-result { background: var(--light-blue); border-radius: var(--radius-md); padding: var(--space-4); margin-top: var(--space-4); text-align: center; }
  .damage-result .damage-range { font-size: var(--font-2xl); font-weight: 800; color: var(--navy); }
  .damage-result .damage-sub { font-size: var(--font-sm); color: var(--muted); margin-top: var(--space-1); }
  .toggle-chip-row { display: flex; flex-wrap: wrap; gap: var(--space-2); margin-top: var(--space-2); }
  .toggle-chip {
    font-size: var(--font-xs);
    font-weight: 600;
    padding: 4px 10px;
    border-radius: var(--radius-pill);
    border: 1px solid var(--border);
    background: var(--surface);
    cursor: pointer;
  }
  .toggle-chip[aria-pressed="true"] { background: var(--blue); border-color: var(--blue); color: #fff; }

  /* --- Top Teams tab: leaderboard tiles + pokepaste import/export --- */
  .pokepaste-box { display: flex; flex-direction: column; gap: var(--space-2); margin-bottom: var(--space-4); }
  .pokepaste-box textarea {
    width: 100%;
    min-height: 140px;
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: var(--font-sm);
    padding: var(--space-2);
    border-radius: var(--radius-sm);
    border: 1px solid var(--border);
    resize: vertical;
  }
  .pokepaste-actions { display: flex; gap: var(--space-2); align-items: center; }
  .pokepaste-status { font-size: var(--font-xs); color: var(--muted); }

  /* --- Buttons --- */
  .btn {
    font: inherit;
    font-size: var(--font-sm);
    font-weight: 600;
    padding: 6px 12px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--navy);
    cursor: pointer;
  }
  .btn:hover { background: var(--light-blue); }
  .btn:disabled { opacity: 0.4; cursor: not-allowed; }
  .btn-primary { background: var(--blue); color: #fff; border-color: var(--blue); }
  .btn-primary:hover { background: var(--navy); }
  .btn-primary:disabled { background: var(--muted); border-color: var(--muted); }
  .btn-remove {
    background: none;
    border: none;
    color: var(--danger);
    font-size: var(--font-md);
    cursor: pointer;
    line-height: 1;
    padding: 2px 6px;
  }
  .btn-remove:hover { text-decoration: underline; }
  .btn-sm { padding: 4px 10px; font-size: var(--font-xs); }

  input[type="text"] {
    padding: 6px 10px;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border);
    font-size: 16px; /* prevents iOS Safari auto-zoom-on-focus */
    width: 100%;
  }

  /* --- Team Builder --- */
  .team-builder-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.3fr) minmax(0, 1fr);
    gap: var(--space-6);
    align-items: start;
  }
  .roster-picker { display: flex; flex-direction: column; gap: var(--space-3); }
  .roster-list {
    list-style: none;
    margin: 0;
    padding: 0;
    max-height: 420px;
    overflow-y: auto;
    border: 1px solid var(--light-blue);
    border-radius: var(--radius-sm);
  }
  .roster-item {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-3);
    border-bottom: 1px solid var(--light-blue);
  }
  .roster-item:last-child { border-bottom: none; }
  .roster-item .roster-info { flex: 1 1 auto; min-width: 0; }
  .roster-item .roster-name { font-weight: 600; }
  .roster-item .roster-sub { font-size: var(--font-xs); color: var(--muted); }

  .team-slots {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-3);
  }
  .team-slot {
    border: 2px solid var(--border);
    border-radius: var(--radius-md);
    padding: var(--space-4);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-1);
    text-align: center;
    min-height: 96px;
    justify-content: center;
  }
  .team-slot.empty { border-style: dashed; color: var(--muted); font-size: var(--font-xs); }
  .team-slot img { width: var(--icon-lg); height: var(--icon-lg); object-fit: contain; }
  .team-slot .slot-name { font-weight: 600; font-size: var(--font-sm); }
  .team-slot .slot-detail { font-size: var(--font-xs); color: var(--muted); }
  .team-slot select { width: 100%; margin-top: 2px; }
  .team-slot .slot-move-selects { display: flex; flex-direction: column; gap: 2px; width: 100%; }

  .speed-order-list { list-style: none; margin: 0; padding: 0; }
  .speed-order-list li {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-1) 0;
  }
  .speed-order-list .speed-value { margin-left: auto; font-weight: 600; }

  .stat-summary-row {
    display: flex;
    gap: var(--space-4);
    flex-wrap: wrap;
    margin-top: var(--space-4);
  }
  .stat-summary-row .stat {
    background: var(--light-blue);
    border-radius: var(--radius-sm);
    padding: var(--space-2) var(--space-3);
    font-size: var(--font-sm);
  }
  .stat-summary-row .stat strong { display: block; font-size: var(--font-lg); color: var(--navy); }

  @media (max-width: 720px) {
    .team-builder-grid { grid-template-columns: 1fr; }
    .team-slots { grid-template-columns: repeat(2, 1fr); }
    .grid-6xn { grid-template-columns: repeat(3, 1fr); }
    .matchup-picker-grid { grid-template-columns: 1fr; }
  }


  .compare-columns { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:16px; margin:16px 0; }
  .compare-card, .expansion-card { background:var(--surface); border:1px solid var(--border); border-radius:var(--radius-md); padding:16px; }
  .compare-card h3, .expansion-card h3 { margin-top:0; }
  .expansion-grid { display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:12px; }
  .link-button { border:0; background:none; color:var(--blue); padding:0; cursor:pointer; text-decoration:underline; }
  .source-note { color:var(--muted); font-size:var(--font-sm); }
  @media (max-width:720px) { .compare-columns, .expansion-grid { grid-template-columns:1fr; } }\n  footer { text-align: center; color: var(--muted); font-size: 0.8rem; padding: 24px; }

  @media (max-width: 720px) {
    main { padding: 16px; }
    .kpi-row { grid-template-columns: repeat(2, 1fr); gap: 12px; }
    .kpi-card { padding: 12px; }
    .kpi-card .value { font-size: 1.2rem; }
  }

  @media (max-width: 480px) {
    header h1 { font-size: 1.3rem; }
    .kpi-row { grid-template-columns: repeat(2, 1fr); gap: 8px; }
    .kpi-card { flex-direction: column; align-items: flex-start; gap: 6px; }
    .controls select { width: 100%; }
    section { padding: var(--space-4); }
    section h2 { margin: 0 calc(var(--space-4) * -1) var(--space-4); padding: var(--space-3) var(--space-4); }
    .grid-6xn { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 360px) {
    .kpi-row { grid-template-columns: 1fr; }
  }
</style>
</head>
<body>
<header>
  <h1>Pokémon Champions — Analytics Dashboard</h1>
  <p>Generated {{ payload.kpis.latest_snapshot_date or "—" }} · built {{ payload.generated_at_utc }}</p>
  {# Freshness/provenance chips: which dataset version and snapshot this
     page was built from, and whether its release gates passed — visible
     without opening the Data & Sources tab. #}
  <div class="header-meta">
    {% if payload.provenance.dataset_version %}
      <span class="header-chip">dataset {{ payload.provenance.dataset_version }}</span>
    {% endif %}
    {% if payload.kpis.latest_snapshot_date %}
      <span class="header-chip">snapshot {{ payload.kpis.latest_snapshot_date }}</span>
    {% endif %}
    {% if payload.provenance.validation_available %}
      <span class="header-chip">{% if payload.provenance.release_blocking_findings %}{{ payload.provenance.release_blocking_findings | length }} gate failure(s){% else %}all gates passing{% endif %}</span>
    {% endif %}
  </div>
</header>
<main>
  <div class="kpi-row">
    <div class="kpi-card">
      <div class="kpi-body">
        <div class="label">Distinct Pokémon Used</div>
        <div class="value">{{ payload.kpis.distinct_pokemon_used }}</div>
      </div>
    </div>
    <div class="kpi-card">
      {% set top_used = payload.kpis.top_used_pokemon %}
      {% set top_used_art = payload.hero_art.get(top_used.pokemon_key) or payload.sprites.get(top_used.pokemon_key) if top_used %}
      {% if top_used_art %}
        <img class="kpi-sprite" src="{{ top_used_art }}" alt="" width="96" height="96" decoding="async">
      {% endif %}
      <div class="kpi-body">
        <div class="label">Most Used</div>
        <div class="value">{% if top_used %}{{ top_used.pokemon_name }}{% else %}—{% endif %}</div>
        <div class="sub">{% if top_used %}{{ top_used.usage_count }} roster appearances{% if top_used.usage_share is not none %} ({{ "%.1f"|format(top_used.usage_share * 100) }}% of meta){% endif %}{% endif %}</div>
      </div>
    </div>
    <div class="kpi-card">
      {% set top_win = payload.kpis.top_win_rate_pokemon %}
      {% set top_win_art = payload.hero_art.get(top_win.pokemon_key) or payload.sprites.get(top_win.pokemon_key) if top_win %}
      {% if top_win_art %}
        <img class="kpi-sprite" src="{{ top_win_art }}" alt="" width="96" height="96" decoding="async">
      {% endif %}
      <div class="kpi-body">
        <div class="label">Top Win Rate</div>
        <div class="value">{% if top_win %}{{ top_win.pokemon_name }}{% else %}—{% endif %}</div>
        <div class="sub">{% if top_win %}{{ "%.1f"|format(top_win.win_rate * 100) }}% ({{ top_win.record_count }} recorded matches){% endif %}</div>
      </div>
    </div>
    <div class="kpi-card">
      <div class="kpi-body">
        <div class="label">Legal Pool (cumulative)</div>
        <div class="value">
          {% for r in payload.kpis.legal_pool_by_regulation %}{{ r.regulation_code }}: {{ r.cumulative_legal_pokemon_count }}{% if not loop.last %} · {% endif %}{% endfor %}
          {% if not payload.kpis.legal_pool_by_regulation %}—{% endif %}
        </div>
        <div class="sub">as of {{ payload.kpis.latest_snapshot_date or "—" }}</div>
      </div>
    </div>
  </div>

  <nav class="tabs" role="tablist">
    <button class="tab-btn" role="tab" data-tab="overview" aria-selected="true">Overview</button>
    <button class="tab-btn" role="tab" data-tab="usage" aria-selected="false">Usage</button>
    <button class="tab-btn" role="tab" data-tab="pokemon-profile" aria-selected="false">Pokémon Profile</button>
    <button class="tab-btn" role="tab" data-tab="archetypes" aria-selected="false">Archetypes</button>
    <button class="tab-btn" role="tab" data-tab="speed-tiers" aria-selected="false">Speed Tiers</button>
    <button class="tab-btn" role="tab" data-tab="matchup" aria-selected="false">Matchup</button>
    <button class="tab-btn" role="tab" data-tab="team-builder" aria-selected="false">Team Builder</button>
    <button class="tab-btn" role="tab" data-tab="top-teams" aria-selected="false">Top Teams</button>
    <button class="tab-btn" role="tab" data-tab="players" aria-selected="false">Players &amp; Regions</button>

    <button class="tab-btn" role="tab" data-tab="compare" aria-selected="false">Compare</button>
    <button class="tab-btn" role="tab" data-tab="core-explorer" aria-selected="false">Core Explorer</button>\n    <button class="tab-btn" role="tab" data-tab="data-sources" aria-selected="false">Data &amp; Sources</button>
  </nav>

  <section class="tab-panel" data-panel="overview">
    <h2>Overview</h2>
    <p class="sub">Top 12 most-used Pokémon this dataset version, ranked by share of the meta.</p>
    <div class="grid-6xn" id="overview-top-12"></div>
  </section>

  <section class="tab-panel" data-panel="usage" hidden>
    <h2>Usage</h2>
    <p class="sub">Real Pokémon Champions tournament data (MunchStats) — every usage and win-rate figure below is computed from actual recorded rosters, not a blend with other sources.</p>
    <div class="controls">
      <label>Tier
        <select id="usage-tier-filter"></select>
      </label>
      <label>Regulation
        <select id="usage-regulation-filter"></select>
      </label>
      <label>Type
        <div class="type-filter-row" id="usage-type-filter"></div>
      </label>
      <label>Role
        <select id="usage-role-filter">
          <option value="">All roles</option>
          <option value="physical">Physical</option>
          <option value="special">Special</option>
          <option value="mixed">Mixed</option>
        </select>
      </label>
      <label>Usage %
        <div class="range-filter">
          <input type="number" id="usage-share-min" min="0" max="100" placeholder="min">
          <span>–</span>
          <input type="number" id="usage-share-max" min="0" max="100" placeholder="max">
        </div>
      </label>
      <label>Speed
        <div class="range-filter">
          <input type="number" id="usage-speed-min" min="0" placeholder="min">
          <span>–</span>
          <input type="number" id="usage-speed-max" min="0" placeholder="max">
        </div>
      </label>
    </div>
    <p class="sub" id="usage-regulation-note" hidden></p>
    <div class="grid-6xn" id="usage-grid"></div>

    <h3>Leaderboards</h3>
    <div class="subtabs" role="tablist">
      <button class="subtab-btn" role="tab" data-subtab="usage-leaders" aria-selected="true">Usage leaders</button>
      <button class="subtab-btn" role="tab" data-subtab="win-rate-leaders" aria-selected="false">Win rate leaders</button>
      <button class="subtab-btn" role="tab" data-subtab="usage-success" aria-selected="false">Success</button>
      <button class="subtab-btn" role="tab" data-subtab="usage-trends" aria-selected="false">Trends</button>
    </div>

    <div class="subtab-panel" data-subpanel="usage-leaders">
      <p class="sub">Ranked by % of meta within the selected tier and filters. Click a column header to re-sort.</p>
      <div class="table-scroll">
        <table id="usage-leaders-table">
          <thead><tr><th>Rank</th><th>Pokémon</th><th class="sortable" data-sort-key="usage_share">% of meta</th></tr></thead>
          <tbody></tbody>
        </table>
      </div>
    </div>

    <div class="subtab-panel" data-subpanel="win-rate-leaders" hidden>
      <div class="controls">
        <label>Min. recorded matches
          <select id="win-rate-min-record-count-filter">
            <option value="5">5+</option>
            <option value="10">10+</option>
            <option value="20">20+</option>
            <option value="50">50+</option>
          </select>
        </label>
      </div>
      <div class="grid-6xn" id="win-rate-grid"></div>
      <div class="table-scroll">
        <table id="win-rate-table">
          <thead><tr><th>Rank</th><th>Pokémon</th><th class="sortable" data-sort-key="win_rate">Win rate</th></tr></thead>
          <tbody></tbody>
        </table>
      </div>
    </div>

    <div class="subtab-panel" data-subpanel="usage-success" hidden>
      <p class="sub">
        Popular is not the same as successful. Raw usage counts a last-place
        team's roster slot exactly like a winner's; these two views weight it
        by where the team actually finished
        (<code>tournament_team.placement</code>).
        <strong>Top cut</strong> counts only appearances on a team that placed
        1&#8211;8; <strong>placement-weighted</strong> weights every appearance
        by 1/placement, so a win counts far more than a 200th with no cutoff
        cliff. The <strong>Movement</strong> column is how many places a
        Pokémon gains or loses versus its raw usage rank — a positive number is
        a quiet top-cut staple, a negative one is a crowd favourite that
        underperforms.
      </p>
      <div class="controls">
        <label>Ranked by
          <select id="usage-success-metric">
            <option value="placement_weighted">Placement-weighted</option>
            <option value="top_cut">Top cut (1&#8211;8)</option>
          </select>
        </label>
      </div>
      <div class="grid-6xn" id="usage-success-grid"></div>
      <div class="table-scroll">
        <table id="usage-success-table">
          <thead><tr><th>Rank</th><th>Pokémon</th><th class="sortable" data-sort-key="weighted_usage_share">Weighted share</th><th class="sortable" data-sort-key="top_cut_usage_share">Top-cut share</th><th class="sortable" data-sort-key="usage_share">Raw usage</th><th class="sortable" data-sort-key="rank_movement">Movement</th></tr></thead>
          <tbody></tbody>
        </table>
      </div>
    </div>

    <div class="subtab-panel" data-subpanel="usage-trends" hidden>
      <p class="sub">Usage by tournament date (<code>tournament_event.event_date</code>), not by extraction snapshot — this shows how usage shifted across events actually played. Change is versus the previous tournament date in the data, not a fixed time window.</p>
      <div class="controls">
        <label>Tournament date
          <select id="usage-trend-date-filter"></select>
        </label>
      </div>
      <div class="grid-6xn" id="usage-trend-grid"></div>
      <div class="table-scroll">
        <table id="usage-trend-table">
          <thead><tr><th>Rank</th><th>Pokémon</th><th class="sortable" data-sort-key="usage_share">% of meta</th><th class="sortable" data-sort-key="usage_share_delta">Change</th></tr></thead>
          <tbody></tbody>
        </table>
      </div>
    </div>
  </section>

  <section class="tab-panel" data-panel="pokemon-profile" hidden>
    <h2>Pokémon Profile</h2>
    <div class="controls">
      <label>Pokémon
        <select id="pokemon-profile-filter"></select>
      </label>
    </div>

    <div id="pokemon-profile-stats" class="empty-state"></div>

    <div class="subtabs" role="tablist">
      <button class="subtab-btn" role="tab" data-subtab="profile-items" aria-selected="true">Items</button>
      <button class="subtab-btn" role="tab" data-subtab="profile-ability">Ability</button>
      <button class="subtab-btn" role="tab" data-subtab="profile-moves">Moves</button>
      <button class="subtab-btn" role="tab" data-subtab="profile-team-cores">Team Cores</button>
    </div>

    <div class="subtab-panel" data-subpanel="profile-items">
      <p class="sub">Top 5 recorded held items. <span id="profile-item-concentration"></span></p>
      <div class="grid-6xn" id="profile-item-grid"></div>
    </div>

    <div class="subtab-panel" data-subpanel="profile-ability" hidden>
      <p class="sub">Top 5 recorded abilities. <span id="profile-ability-concentration"></span></p>
      <div class="grid-6xn" id="profile-ability-grid"></div>
    </div>

    <div class="subtab-panel" data-subpanel="profile-moves" hidden>
      <p class="sub">Top 15 recorded moves, with full mechanics.</p>
      <div class="table-scroll">
        <table id="profile-move-table">
          <thead>
            <tr>
              <th>Move</th>
              <th class="sortable" data-sort-key="move_share">Usage</th>
              <th>Category</th>
              <th class="sortable" data-sort-key="power">Power</th>
              <th class="sortable" data-sort-key="accuracy">Accuracy</th>
              <th class="sortable" data-sort-key="pp">PP</th>
              <th class="sortable" data-sort-key="priority">Priority</th>
              <th>Effect</th>
            </tr>
          </thead>
          <tbody></tbody>
        </table>
      </div>
    </div>

    <div class="subtab-panel" data-subpanel="profile-team-cores" hidden>
      <p class="sub">
        Which Pokémon most often share a team with this Pokémon.
        <strong>Co-occurrence</strong> is the raw pairing rate, which mostly
        re-ranks whoever is individually popular; <strong>synergy</strong>
        ranks by lift — how much more often the pair appears together than
        their individual popularity alone predicts (&times;1.0 is exactly
        chance). Synergy is restricted to pairs seen on at least 5 real teams,
        since lift is noisy below that.
      </p>
      <div class="controls">
        <label>Core size
          <select id="profile-team-core-size">
            <option value="pairs">Pairs</option>
            <option value="triples">Triples</option>
          </select>
        </label>
        <label>Ranked by
          <select id="profile-team-core-metric">
            <option value="co_occurrence">Co-occurrence</option>
            <option value="synergy">Synergy (lift)</option>
          </select>
        </label>
      </div>
      <div class="grid-6xn" id="profile-team-core-grid"></div>
    </div>
  </section>

  <section class="tab-panel" data-panel="archetypes" hidden>
    <h2>Detected Archetypes</h2>
    <p class="sub">
      Experimental, data-derived team identities anchored by three-Pokémon
      cores from real Champions rosters. Qualifying cores appear on at least
      five teams and have above-chance three-way and average pair lift. Cores
      sharing two members consolidate under their strongest neighbour; teams
      receive one primary assignment and may retain one near-tied secondary
      assignment. Neutral core names are deliberate—composition alone does
      not prove an editorial label such as “Rain” or “Balance.”
    </p>
    <div class="grid-6xn" id="detected-archetype-grid"></div>
  </section>

  <section class="tab-panel" data-panel="speed-tiers" hidden>
    <h2>Speed tiers</h2>
    <p class="sub">
      Who actually moves first, under the scenario you pick. Everything but
      "Base stat" is <strong>max Speed investment</strong> at Level 50 (252
      EVs, a beneficial nature, a 31 IV) — the documented convention in
      <code>pokemon_champions_profile</code>, since no source publishes real
      per-roster EV spreads. Choose an opposing Speed to see who outruns it.
    </p>
    <div class="controls">
      <label>Scenario
        <select id="speed-tiers-scenario">
          <option value="max_investment_speed">Max investment</option>
          <option value="base_speed">Base stat</option>
          <option value="plus_one_speed">+1 stage / Choice Scarf (&times;1.5)</option>
          <option value="plus_two_speed">+2 stages / Tailwind (&times;2)</option>
          <option value="scarf_tailwind_speed">Scarf + Tailwind (&times;3)</option>
        </select>
      </label>
      <label>Speed
        <div class="range-filter">
          <input type="number" id="speed-tiers-min" min="0" placeholder="min">
          <span>–</span>
          <input type="number" id="speed-tiers-max" min="0" placeholder="max">
        </div>
      </label>
      <label>Outruns
        <input type="number" id="speed-tiers-benchmark" min="0" placeholder="e.g. 180">
      </label>
      <label>Type
        <div class="type-filter-row" id="speed-tiers-type-filter"></div>
      </label>
    </div>
    <p class="sub" id="speed-tiers-benchmark-note" hidden></p>
    <div class="grid-6xn" id="speed-grid"></div>
    <div class="table-scroll">
      <table id="speed-tiers-table">
        <thead>
          <tr>
            <th>Rank</th><th>Pokémon</th>
            <th class="sortable" data-sort-key="base_speed">Base</th>
            <th class="sortable" data-sort-key="max_investment_speed">Max</th>
            <th class="sortable" data-sort-key="plus_one_speed">+1 / Scarf</th>
            <th class="sortable" data-sort-key="plus_two_speed">+2 / Tailwind</th>
            <th class="sortable" data-sort-key="scarf_tailwind_speed">Scarf + TW</th>
            <th>Tier</th>
            <th class="sortable" data-sort-key="usage_share">Usage</th>
            <th class="sortable" data-sort-key="win_rate">Win rate</th>
          </tr>
        </thead>
        <tbody></tbody>
      </table>
    </div>
  </section>

  <section class="tab-panel" data-panel="matchup" hidden>
    <h2>Matchup</h2>
    <p class="sub">
      Real head-to-head records, type effectiveness, teammate co-usage, and a
      stats/setup/weather-aware damage calculator — see
      docs/design-system.md's "Matchup tab" for what this does and doesn't
      model. Head-to-head is measured <strong>team vs team</strong>: no source
      publishes which Pokémon were actually brought to a given match.
    </p>
    <div class="matchup-picker-grid">
      <div class="matchup-side">
        <h4>Attacker</h4>
        <label>Pokémon <select id="matchup-attacker-select"></select></label>
        <div id="matchup-attacker-type" class="type-badge-row"></div>
        <div id="matchup-attacker-stats" class="desc-text"></div>
        <label>Move <select id="matchup-move-select"></select></label>
        <div id="matchup-move-desc" class="desc-text"></div>
        <h4>Setup</h4>
        <div id="matchup-attacker-stages"></div>
      </div>
      <div class="matchup-side">
        <h4>Defender</h4>
        <label>Pokémon <select id="matchup-defender-select"></select></label>
        <div id="matchup-defender-type" class="type-badge-row"></div>
        <div id="matchup-defender-stats" class="desc-text"></div>
        <h4>Setup</h4>
        <div id="matchup-defender-stages"></div>
      </div>
    </div>
    <div class="controls">
      <label>Weather
        <select id="matchup-weather">
          <option value="">None</option>
          <option value="rain">Rain</option>
          <option value="sun">Harsh Sunlight</option>
          <option value="sand">Sandstorm</option>
          <option value="snow">Snow</option>
        </select>
      </label>
    </div>
    <div class="toggle-chip-row" id="matchup-toggle-row"></div>
    <div class="damage-result" id="matchup-damage-result"></div>

    <h3>Type effectiveness</h3>
    <p class="sub">The attacker's type(s) against the defender's type(s).</p>
    <div class="type-effect-grid" id="matchup-type-grid"></div>

    <h3>Head-to-head record</h3>
    <p class="sub">
      How often teams fielding the attacker actually beat teams fielding the
      defender, from real tournament pairings. Ranked by a 95% Wilson lower
      bound, so a 1&#8209;0 record doesn't outrank a well-established one.
      This is a <strong>team-level</strong> result, not a claim that these two
      Pokémon fought each other.
    </p>
    <div class="damage-result" id="matchup-h2h-headline"></div>
    <h4>Matchup profile</h4>
    <p class="sub">Each side's overall record across every decided match its teams played, and its single best and worst opponent — both chosen by Wilson lower bound over a minimum sample, so a 1&#8209;0 record never reads as a best matchup.</p>
    <div class="matchup-picker-grid" id="matchup-summary"></div>
    <h4>Defender's toughest opponents</h4>
    <p class="sub">Pokémon whose teams beat the defender's teams most reliably (minimum 10 recorded matches).</p>
    <div class="grid-6xn" id="matchup-h2h-grid"></div>

    <h3>Co-usage</h3>
    <p class="sub">Pokémon most often teamed with the defender in real tournament rosters. This is a teammate-pairing view, not a matchup signal — the head-to-head section above is the matchup signal.</p>
    <div class="grid-6xn" id="matchup-co-usage-grid"></div>
  </section>

  <section class="tab-panel" data-panel="team-builder" hidden>
    <h2>Team builder</h2>
    <p class="sub">
      Build a roster of up to 6 from the current legal pool, with each
      pick's stats, top move, and top ability shown right on its slot. Your
      team is saved in this browser (not shared or uploaded anywhere).
    </p>
    <div class="team-builder-grid">
      <div class="roster-picker">
        <h3>Legal pool</h3>
        <div class="controls">
          <label>Search
            <input type="text" id="team-builder-search" placeholder="Filter by name…">
          </label>
          <label>Sort by
            <select id="team-builder-sort">
              <option value="usage">Usage</option>
              <option value="win_rate">Win rate</option>
              <option value="speed">Speed</option>
            </select>
          </label>
          <label>Type
            <div class="type-filter-row" id="team-builder-type-filter"></div>
          </label>
        </div>
        <ul class="roster-list" id="team-builder-available"></ul>
      </div>
      <div class="team-roster">
        <h3>Your team (<span id="team-builder-count">0</span>/6)</h3>
        <div class="team-slots" id="team-builder-slots"></div>
        <div class="pokepaste-actions">
          <button class="btn btn-sm" id="team-builder-clear" type="button">Clear team</button>
          <button class="btn btn-sm" id="team-builder-export" type="button">Export as pokepaste text</button>
        </div>
        <textarea id="team-builder-export-output" class="pokepaste-box" style="display:none" readonly></textarea>

        <h3>Speed order</h3>
        <p class="sub">Your team's picks, fastest first — who moves before whom.</p>
        <ol class="speed-order-list" id="team-builder-speed-order"></ol>

        <div class="stat-summary-row" id="team-builder-summary"></div>
      </div>
    </div>
  </section>

  <section class="tab-panel" data-panel="top-teams" hidden>
    <h2>Import a team</h2>
    <p class="sub">
      Paste a Showdown/pokepast.es-format team export below (the same text
      you'd paste to create a pokepast.es paste) to load it into Team
      Builder. Nothing is uploaded — parsing happens in your browser.
    </p>
    <div class="pokepaste-box">
      <textarea id="pokepaste-input" placeholder="Paste a Showdown-format team export here…"></textarea>
      <div class="pokepaste-actions">
        <button class="btn btn-primary btn-sm" id="pokepaste-load" type="button">Load into Team Builder</button>
        <span class="pokepaste-status" id="pokepaste-status"></span>
      </div>
    </div>

    <h2>Top Teams</h2>
    <div class="subtabs" role="tablist">
      <button class="subtab-btn" role="tab" data-subtab="tt-leaderboard" aria-selected="true">By win rate</button>
      <button class="subtab-btn" role="tab" data-subtab="tt-converged" aria-selected="false">Converged lists</button>
    </div>

    <div class="subtab-panel" data-subpanel="tt-leaderboard">
      <p class="sub">Real Pokémon Champions tournament teams (MunchStats), ranked by reported win rate — distinct from the curated Pro Team Gallery below.</p>
      <div class="grid-6xn" id="top-teams-grid"></div>
    </div>

    <div class="subtab-panel" data-subpanel="tt-converged" hidden>
      <p class="sub">
        Whole team compositions the field independently converged on
        (Limitless VGC), ranked by how many distinct players fielded the
        same six Pokémon. <strong>Day-2 cut only</strong> — Limitless
        publishes the teams that made the cut, about 93 distinct Pokémon
        against 203 across the full Champions roster, so this shows what
        converged at the top of the field rather than the meta as a whole.
      </p>
      <div class="grid-6xn" id="converged-lists-grid"></div>
    </div>

    <h3>Pro Team Gallery</h3>
    <p class="sub">
      Curated real tournament teams for reference/inspiration — not part of
      your saved roster. "Load into my builder" copies a gallery team's
      Pokémon into Team Builder (skipping any no longer in the legal pool).
    </p>
    <div class="gallery-grid" id="pro-team-gallery"></div>
  </section>

  <section class="tab-panel" data-panel="players" hidden>
    <h2>Players &amp; Regions</h2>
    <p class="sub">
      Who plays what. Both views are computed across a player's or a
      country's <em>entire</em> recorded Champions history, not a single
      event. Country codes are MunchStats' own two-letter codes, and a team
      only appears here if its player reported one.
    </p>
    <div class="subtabs" role="tablist">
      <button class="subtab-btn" role="tab" data-subtab="players-regions" aria-selected="true">By region</button>
      <button class="subtab-btn" role="tab" data-subtab="players-signature" aria-selected="false">By player</button>
    </div>

    <div class="subtab-panel" data-subpanel="players-regions">
      <div class="controls">
        <label>Country
          <select id="players-country-filter"></select>
        </label>
      </div>
      <p class="sub" id="players-country-note"></p>
      <div class="grid-6xn" id="players-country-grid"></div>
      <div class="table-scroll">
        <table id="players-country-table">
          <thead><tr><th>Rank</th><th>Pokémon</th><th class="sortable" data-sort-key="usage_share">Share of this country's picks</th></tr></thead>
          <tbody></tbody>
        </table>
      </div>
    </div>

    <div class="subtab-panel" data-subpanel="players-signature" hidden>
      <p class="sub">
        A player's most-fielded Pokémon across every team of theirs on record.
        Restricted to players with at least 2 recorded teams — a "signature"
        claim from someone with a single recorded team is just that team's
        roster. Only three Champions events exist anywhere, so two is a
        meaningful bar here rather than a low one: exactly one player in the
        whole dataset has three.
      </p>
      <div class="controls">
        <label>Player
          <select id="players-player-filter"></select>
        </label>
      </div>
      <p class="sub" id="players-player-note"></p>
      <div class="grid-6xn" id="players-signature-grid"></div>
      <h3>Most committed specialists</h3>
      <p class="sub">
        Players who brought their top pick to the largest share of their own
        recorded <em>teams</em> — 100% means it was on every team they
        fielded. Deliberately not ranked by share of roster slots: a Pokémon
        can only appear once per six-slot team, so that share is capped at
        16.7% and ties everyone who is fully committed.
      </p>
      <div class="table-scroll">
        <table id="players-specialists-table">
          <thead><tr><th>Rank</th><th>Player</th><th>Country</th><th>Signature Pokémon</th><th class="sortable" data-sort-key="team_share">Share of their teams</th><th>Fielded in</th></tr></thead>
          <tbody></tbody>
        </table>
      </div>
    </div>
  </section>


  <section class="tab-panel" data-panel="compare" hidden>
    <h2>Compare Pokémon</h2>
    <p class="section-intro">Compare sourced competitive profiles side by side, then share the exact view with a URL.</p>
    <div class="filters">
      <label>Pokémon A <select id="compare-a"></select></label>
      <label>Pokémon B <select id="compare-b"></select></label>
      <button class="button" id="compare-copy" type="button">Copy view link</button>
    </div>
    <div id="compare-results"></div>
  </section>
  <section class="tab-panel" data-panel="core-explorer" hidden>
    <h2>Team Core Explorer</h2>
    <p class="section-intro">Search observed pair and triple composition patterns, with sample size and lift kept visible.</p>
    <div class="filters">
      <label>Anchor Pokémon <select id="core-anchor"></select></label>
      <label>Core size <select id="core-size"><option value="pair">Pairs</option><option value="triple">Triples</option></select></label>
    </div>
    <div id="core-results"></div>
  </section>\n  <section class="tab-panel" data-panel="data-sources" hidden>
    <h2>Data &amp; Sources</h2>
    <p class="sub">
      Where every number on this dashboard comes from, and whether it passed
      the release gates in <code>docs/dataset-spec.md</code>. Provenance is
      mandatory in this dataset — every record carries its source, URL,
      upstream record id and extraction timestamp — so it is reported here
      rather than left implicit.
    </p>

    <div class="subtabs" role="tablist">
      <button class="subtab-btn" role="tab" data-subtab="ds-sources" aria-selected="true">Sources</button>
      <button class="subtab-btn" role="tab" data-subtab="ds-gates" aria-selected="false">Release gates</button>
      <button class="subtab-btn" role="tab" data-subtab="ds-assets" aria-selected="false">Image coverage</button>
      <button class="subtab-btn" role="tab" data-subtab="ds-agreement" aria-selected="false">Source agreement</button>
      <button class="subtab-btn" role="tab" data-subtab="ds-caveats" aria-selected="false">Caveats</button>
    </div>

    <div class="subtab-panel" data-subpanel="ds-sources">
      <div class="table-scroll">
        <table id="ds-sources-table">
          <thead><tr><th>Source</th><th>Endpoint</th><th>Availability</th><th class="sortable" data-sort-key="request_success_rate">Request success</th><th class="sortable" data-sort-key="rows_written">Rows</th><th class="sortable" data-sort-key="required_field_null_rate">Null rate</th><th>Checked</th></tr></thead>
          <tbody></tbody>
        </table>
      </div>
    </div>

    <div class="subtab-panel" data-subpanel="ds-gates" hidden>
      <p class="sub" id="ds-gates-status"></p>
      <div class="table-scroll">
        <table id="ds-gates-table">
          <thead><tr><th>Gate</th><th>Check</th><th>What it measures</th><th>Threshold</th><th>Value</th><th>Status</th></tr></thead>
          <tbody></tbody>
        </table>
      </div>
    </div>

    <div class="subtab-panel" data-subpanel="ds-assets" hidden>
      <p class="sub">
        How much of the imagery this dashboard references actually resolved.
        A Pokémon with no resolvable image degrades to its name in text —
        never a broken image — so a shortfall here is otherwise invisible.
      </p>
      <div class="grid-6xn" id="ds-assets-grid"></div>
    </div>

    <div class="subtab-panel" data-subpanel="ds-agreement" hidden>
      <p class="sub">
        Two independent sources publish Champions rosters — MunchStats and
        Limitless VGC. This is how often they agree, per event: what share of
        the players covered by both have an identical six-Pokémon roster, and
        what share of individual slots match. Disagreement is not
        necessarily either source being wrong (rosters get corrected, and
        coverage differs), but a sharp drop is worth knowing about before
        trusting a number downstream.
      </p>
      <div class="table-scroll">
        <table id="ds-agreement-table">
          <thead><tr><th>Event</th><th class="sortable" data-sort-key="covered_players">Players covered by both</th><th class="sortable" data-sort-key="exact_agreement_rate">Exact roster agreement</th><th class="sortable" data-sort-key="slot_agreement_rate">Slot agreement</th></tr></thead>
          <tbody></tbody>
        </table>
      </div>
    </div>

    <div class="subtab-panel" data-subpanel="ds-caveats" hidden>
      <p class="sub">
        Standing limitations that apply to figures elsewhere on this
        dashboard. Each is a property of what the sources publish, not a bug
        to be fixed here.
      </p>
      <ul class="caveat-list">
        <li>
          <strong>Head-to-head is team vs team, never Pokémon vs Pokémon.</strong>
          No source publishes a per-battle log naming which four of six were
          actually brought, so a "matchup" figure means teams containing A
          beat teams containing B — it is attributed to the whole roster.
        </li>
        <li>
          <strong>Matchup appearance counts overstate sample roughly 6×</strong>
          for the same reason: every pairing is counted against all six
          Pokémon on each side.
        </li>
        <li>
          <strong>The legal pool is an upper bound.</strong> PokéBase
          publishes no removal signal, so the cumulative legal-Pokémon count
          can only grow — a later-banned Pokémon is never subtracted.
        </li>
        <li>
          <strong>Signature share is structurally capped at 16.7%</strong> —
          a Pokémon can appear only once on a six-slot team — which is why
          players are ranked by share of their <em>teams</em> instead.
        </li>
        <li>
          <strong>Everything here is Champions-format only.</strong> The
          tournament corpus also covers standard VGC events; every usage and
          win-rate figure is filtered to Champions events first. Blending the
          two would be comparing different games.
        </li>
        <li>
          <strong>No EV or IV spreads exist anywhere in this dataset.</strong>
          Official team sheets publish ability, held item, nature and moves
          and nothing more. The damage calculator therefore assumes a
          maximally-invested level-50 baseline on both sides rather than any
          real spread.
        </li>
        <li>
          <strong>Legality has a single snapshot date</strong>, so nothing on
          this dashboard trends legality over time; usage trends are measured
          across tournament dates instead.
        </li>
        <li>
          <strong>Champions stat deltas are all zero.</strong> No rebalance
          has been published yet, so every Pokémon's Champions stats still
          equal its canonical stats.
        </li>
      </ul>
    </div>
  </section>

</main>
<footer>Pokémon Champions Competitive Data Platform · static dashboard built by pipelines/dashboard/</footer>

<script>window.DASHBOARD_DATA = {{ data_json | safe }};</script>
<script src="app.js"></script>
<script src="matchup.js"></script>
<script src="teams.js"></script>\n<script src="expansions.js"></script>
</body>
</html>
